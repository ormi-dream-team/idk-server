spring:
  application:
    name: idk-server
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:h2:mem:test
  h2:
    console:
      enabled: true
      path: /h2-console
springdoc:
  swagger-ui:
    use-root-path: true